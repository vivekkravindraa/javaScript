var numbers = [10,15,20,25];
for(var i = 0; i < numbers.length; i++) {
    if(numbers[i] % 2 === 0) {
        console.log(numbers[i], 'is even');
    } else {
        console.log(numbers[i], 'odd even');
    }
}